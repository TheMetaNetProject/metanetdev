To run the pos tagger, you pass a persian raw text file.  It's
recommended that you tokenize the file (splitting the punctuations) before hand:

Usage:
    python runTagger.py persian-raw-text-file

Example: 
    python runTagger.py samplePersian.txt

